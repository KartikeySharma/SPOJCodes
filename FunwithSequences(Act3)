#include<stdio.h>

int main()
{
	int arr1[1000],arr2[1000];
	int i,j,m,n;
	scanf("%d",&m);
	for(i=0;i<m;i++)
	scanf("%d",&arr1[i]);
	scanf("%d",&n);
	for(j=0;j<n;j++)
	scanf("%d",&arr2[j]);
	
	int val;
	if(n<m)
	 val=n;
	else
	val=m;
	for(i=0;i<val;i++)
	{
		j=i;
		if(arr1[i]==arr2[j])
			printf("%d\t",i+1);
	}
} 
